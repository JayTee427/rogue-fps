{
  "name": "hollow-signal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A roguelike first-person shooter in the browser. Every run is a different game.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "eslint src tests && vitest run",
    "lint": "eslint src tests",
    "test:watch": "vitest",
    "test:core": "vitest run tests/hidden",
    "test:integration": "vitest run tests/integration"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "eslint": "^9.39.5",
    "vercel": "^59.1.3",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
